apiVersion: onnxd/v1alpha1
kind: Model
name: piper-ru
version: 1.0.0

source:
  repo: https://github.com/nikita-popov/examples-gonnx.git
  ref: master
  dir: piper-ru

runtime:
  engine: onnxruntime
  model: ./model.onnx
  providers:
    - CPUExecutionProvider
  session:
    interOpThreads: 1
    intraOpThreads: 4
    graphOptimizationLevel: all

handler:
  type: python
  entrypoint: ./handler.py
  callable: app
  python:
    requirements: ./requirements.txt

interface:
  inputSchema:
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    properties:
      text:
        type: string
        description: Russian text to synthesize.
      speaker_id:
        type: integer
        minimum: 0
        default: 0
        description: Speaker index (for multi-speaker models).
      length_scale:
        type: number
        minimum: 0.1
        maximum: 5.0
        default: 1.0
        description: Speaking speed. >1 slower, <1 faster.
      noise_scale:
        type: number
        minimum: 0.0
        maximum: 1.0
        default: 0.667
        description: Variation in pitch/energy.
      noise_w:
        type: number
        minimum: 0.0
        maximum: 1.0
        default: 0.8
        description: Variation in phoneme duration.
    required: [text]
    additionalProperties: false
  outputSchema:
    $schema: https://json-schema.org/draft/2020-12/schema
    type: object
    properties:
      audio_b64:
        type: string
        contentEncoding: base64
        description: Base64-encoded WAV audio.
      sample_rate:
        type: integer
        description: Sample rate in Hz (from model config).
    required: [audio_b64, sample_rate]

policy:
  startupTimeoutMs: 60000
  predictTimeoutMs: 30000
  maxConcurrency: 4
  idleUnloadSeconds: 900
  network: disabled
  filesystem:
    mode: read-only

security:
  allowUnsigned: true
  allowedHosts: []

# System-level dependencies that must be present on the host.
# gonnxctl pull will emit a warning for each missing dep.
# gonnxctl load will set the worker state to "degraded" if any are missing.
system:
  deps:
    - name: espeak-ng
      check: "espeak-ng --version"
      hint: "apt install espeak-ng"

assets:
  - id: model
    url: https://huggingface.co/rhasspy/piper-voices/resolve/main/ru/ru_RU/irina/medium/ru_RU-irina-medium.onnx
    sha256: 8ff38212d23da300bbe3705c645e6e5b9475f0bfde01558eb17813e22acaaaaa
    dest: ./model.onnx
  - id: config
    url: https://huggingface.co/rhasspy/piper-voices/resolve/main/ru/ru_RU/irina/medium/ru_RU-irina-medium.onnx.json
    sha256: c2ec28bb38e2b59e93b959b3e40348c1afebbd272f30fed5d41205d08e98a9d7
    dest: ./model.onnx.json
